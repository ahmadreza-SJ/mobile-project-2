<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/layout_main">

    <fragment
        android:id="@+id/layout_main"
        android:name="com.example.project2.MainActivity"
        android:label="Blank"
        tools:layout="@layout/activity_main">

        <action
            android:id="@+id/main_to_about"
            app:destination="@id/layout_about"
            />
    </fragment>
    <fragment
        android:id="@+id/layout_about"
        android:name="com.example.project2.MainActivity"
        android:label="Blank"
        tools:layout="@layout/about_page">

        <action
            android:id="@+id/about_to_main"
            app:destination="@id/layout_home" />
    </fragment>
</navigation>